# Built Max externals

Эта папка заполняется post-build командой CMake при включённой опции
`CONSOLIDATOR_COPY_BUILT_EXTERNALS_TO_MAX=ON`.

Артефакты являются локальными копиями сборки и не заменяют исходный native-код.
