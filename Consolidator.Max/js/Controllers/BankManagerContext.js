function BankManagerContext(viewModel, state, analyzer, selectedBank, instanceId) {
    this.viewModel = viewModel;
    this.state = state;
    this.analyzer = analyzer;
    this.selectedBank = selectedBank;
    this.instanceId = instanceId;
}
